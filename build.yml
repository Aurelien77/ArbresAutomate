on: push
jobs:
 build-node:
 runs-on: unbuntu-lasted
 container: node:20
 steps:
  - run: node --version
  - run: npm --version
  - uses: actions/checkout@v4
  - run: npm install
  - run: node server.js
